---
// Fundations
import Text from "@/components/fundations/elements/Text.astro";
import Button from "@/components/fundations/elements/Button.astro";
import Wrapper from "@/components/fundations/containers/Wrapper.astro";
// Components
import CasesCard from "@/components/cases/CasesCard.astro";
// Content
import { getCollection } from "astro:content";
const allCases = await getCollection("cases");
// Select first 3 cases to highlight
const casesCount = allCases.length;
---
<section class="bg-base-50">
  <Wrapper variant="standard" class="py-24">
    <div class="grid grid-cols-1 lg:grid-cols-4 items-start gap-y-8">
      <Text
        tag="p"
        variant="textXS"
        class="text-secondary-500 tracking-wide uppercase"
      >
        Success Stories
      </Text>
      <div class="lg:col-span-3">
        <Text
          tag="h2"
          variant="displayLG"
          class="text-accent-800 font-display text-balance lg:col-span-2"
        >
          Farm Success Stories
        </Text>
        <Text tag="p" variant="textLG" class="text-base-600 mt-4">
          Explore our successful farm transformations and see how we've helped agricultural producers
          achieve operational excellence through IoT automation across various crops and
          farm sizes.
        </Text>
      </div>
    </div>
    <div class="grid grid-cols-1 lg:grid-cols-2 mt-12 lg:mt-24 gap-8">
      {allCases.slice(0, 2).map((caseStudy) => <CasesCard post={caseStudy} />)}
    </div>
    <div class="bg-white p-8 lg:p-12 mt-8">
      <div
        class="flex flex-col lg:flex-row items-start lg:items-center justify-between gap-6"
      >
        <div>
          <Text
            tag="h3"
            variant="displaySM"
            class="text-accent-800 font-display"
          >
            Explore all success stories
          </Text>
          <Text tag="p" variant="textSM" class="text-base-600 mt-1">
            Dive into {casesCount}+ detailed farm transformations and measurable results.
          </Text>
        </div>
        <Button
          isLink={true}
          href="/cases"
          variant="muted"
          size="sm"
          class="w-full lg:w-fit"
        >
          View all success stories
        </Button>
      </div>
    </div>
  </Wrapper>
</section>
